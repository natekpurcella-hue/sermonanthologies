# Video generation dependencies
# Install with: pip3 install -r pipeline/requirements_video.txt
# Node dependency (puppeteer) is already available globally via npm

openai-whisper>=20231117
librosa>=0.10.0
soundfile>=0.12.1
numpy>=1.24.0

# PIL is already installed (Pillow) — used by existing generate_long_video.py
